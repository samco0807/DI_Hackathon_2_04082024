const { db } = require("../config/dbpg.js");

const getAllTransactions = () => {
  third;
};

const insertTransaction = () => {
  third;
};

module.exports = {
  getAllTransactions,
  insertTransaction,
};
